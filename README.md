# Save_code_easily_script
A browser console script that enhances code block interaction on any webpage. It automatically detects code blocks (from Markdown fences, &lt;pre>&lt;code> tags, or plain text), identifies their programming language, and provides a floating UI with save/download capabilities and an interactive block navigator.
